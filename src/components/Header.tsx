import { ThemeToggle } from "./ThemeToggle";

export function Header() {
  return (
    <header>
      <nav>
        <h1>💰 División de Gastos</h1>
        <ThemeToggle />
      </nav>
    </header>
  );
}
